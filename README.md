# Eurooptik-Data-Analysis

This project uses Google Sheets API Key for reading data from google sheets, instead of saving the data locally as Excel/CSV.
A JSON file (credentials.json) is needed for running the script.
